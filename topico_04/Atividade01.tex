\documentclass[a4paper,12pt]{article}
	\usepackage[ansinew]{inputenc}
	\usepackage{ae} % O pacote ae contém a instrução \usepackage[T1]{fontenc}. Por isso você não precisa dela aqui.
\begin{document}

	% Conforme a primeira regra básica, um espaço é tão bom quanto dez. Logo, entre as letras "a" e "b" abaixo existe apenas um espaço (uma pergunta: você reparou que as diversas linhas deste comentário não têm % no começo delas? Você sabe porquê? Se não, pergunte ao professor).
	a          b

	% No entanto, se preenchermos este espaço com grupos vazios, quebraremos a sequência de espaços seguidos (no arquivo de instrução). Neste caso os diversos espaços são preservados, evidenciando o fato de que um grupo vazio não é a mesma coisa que grupo nenhum.
	a {} {} {} b

\end{document}