\documentclass[a4paper,12pt]{article}
	\usepackage{graphicx}
	
	% Ao invés de indicar o caminho relativo de cada uma das figuras, você pode definir uma série de pastas, relativas ao arquivo tex, onde o LaTeX (o driver que insere a figura, na verdade) deve procurar. Este comando só pode ser utilizado no preâmbulo.
	\graphicspath{
		{Figuras/EPS/}
		{Figuras/JPG/}
		{Figuras/PDF/}
		{Figuras/PNG/}
		}
			
\begin{document}

	% Se não fornecermos a extensão do arquivo que contém a figura, o LaTeX procurará por uma figura compatível com o perfil de compilação (output profile do TeXnicCenter) na pasta atual e nas pastas listadas em \graphicspath (se estiver presente).

	\noindent\hrulefill
	\includegraphics[width=0.3\textwidth]{Tei-Gi}%
	\hrulefill
	
	% Ao informar apenas a largura (chave width) OU a altura (height), a outra é alterada automaticamente de modo a manter a relação entre altura e largura.
		
\end{document}